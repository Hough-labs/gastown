From 29a006dbba4c80850aa35171b1e089db8de6686e Mon Sep 17 00:00:00 2001
From: mayor <steve.yegge@gmail.com>
Date: Sun, 12 Apr 2026 15:08:24 -0700
Subject: [PATCH 063/110] =?UTF-8?q?fix:=20unbreak=20main=20(sheriff)=20?=
 =?UTF-8?q?=E2=80=94=20duplicate=20target=20decl,=20unused=20var,=20missin?=
 =?UTF-8?q?g=20rigPrefix?=
MIME-Version: 1.0
Content-Type: text/plain; charset=UTF-8
Content-Transfer-Encoding: 8bit

Three tiny fixes to unblock CI, all from recent PR merges:

1. internal/beads/beads_agent.go — PR #3520 ("route cross-rig agent bead
   creation from town root") left two `target := b` declarations in
   CreateAgentBead; the second is a merge artifact. Drop it; the first block
   (townRoot-based routing) is the intended strategy per the PR title.

2. internal/beads/beads_types.go — Same PR #3520 added `var found string` in
   FindTownRoot but never used it (the function uses `candidate` instead).
   Drop the unused declaration.

3. internal/cmd/formula.go — PR #3613 ("add --set flag and fix bead prefix
   for convoy formulas") referenced `beadPrefix` on line 466, which is not
   declared anywhere. Rename to `rigPrefix` — the variable declared at
   line 451 in the same scope, which matches the surrounding code.

All of main's recent CI runs have been failing on these errors. Verified
locally with `go build ./...` + `go vet ./...` — both clean after fixes.
---
 internal/beads/beads_agent.go | 9 ---------
 internal/beads/beads_types.go | 1 -
 internal/cmd/formula.go       | 2 +-
 3 files changed, 1 insertion(+), 11 deletions(-)

diff --git a/internal/beads/beads_agent.go b/internal/beads/beads_agent.go
index 00301246..a96af7f3 100644
--- a/internal/beads/beads_agent.go
+++ b/internal/beads/beads_agent.go
@@ -248,15 +248,6 @@ func (b *Beads) CreateAgentBead(id, title string, fields *AgentFields) (*Issue,
 		return a
 	}
 
-	// Create agent bead in the target database. Use a routed Beads instance
-	// when the bead's prefix routes to a different rig than our own database.
-	// Without this, agent beads for rig polecats (e.g., be-beads-polecat-rust)
-	// would be created in the wrong database, failing type validation.
-	target := b
-	if targetDir != b.getResolvedBeadsDir() {
-		target = NewWithBeadsDir(filepath.Dir(targetDir), targetDir)
-	}
-
 	out, err := target.run(buildArgs()...)
 	if err != nil {
 		out, err = target.run(buildArgs()...)
diff --git a/internal/beads/beads_types.go b/internal/beads/beads_types.go
index 8842b48a..a6cd706e 100644
--- a/internal/beads/beads_types.go
+++ b/internal/beads/beads_types.go
@@ -39,7 +39,6 @@ var (
 // shadow the real town root above them.
 // Returns empty string if not found (reached filesystem root).
 func FindTownRoot(startDir string) string {
-	var found string
 	dir := startDir
 	candidate := ""
 	for {
diff --git a/internal/cmd/formula.go b/internal/cmd/formula.go
index b0a4b020..ec2226e9 100644
--- a/internal/cmd/formula.go
+++ b/internal/cmd/formula.go
@@ -463,7 +463,7 @@ func executeConvoyFormula(f *formula.Formula, formulaName, targetRig string) err
 	}
 
 	// Step 1: Create convoy bead
-	convoyID := fmt.Sprintf("%s-cv-%s", beadPrefix, generateFormulaShortID())
+	convoyID := fmt.Sprintf("%s-cv-%s", rigPrefix, generateFormulaShortID())
 	convoyTitle := fmt.Sprintf("%s: %s", formulaName, f.Description)
 	if len(convoyTitle) > 80 {
 		convoyTitle = convoyTitle[:77] + "..."
-- 
2.43.0

